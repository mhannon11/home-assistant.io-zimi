{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate",
            "type": "shell",
            "command": "bundle exec rake generate --trace",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Preview",
            "type": "shell",
            "command": "bundle exec rake preview --trace",
            "group": {
                "kind": "test",
                "isDefault": true,
            }
        }
    ]
}